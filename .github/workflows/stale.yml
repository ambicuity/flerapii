name: Stale Issue Management

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

permissions: read-all

jobs:
  stale:
    name: Mark and Close Stale Issues
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write

    steps:
      - uses: actions/stale@v10
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}

          days-before-issue-stale: 30
          days-before-issue-close: 14
          stale-issue-label: 'stale'
          stale-issue-message: |
            This issue has been inactive for 30 days.
            If it is still relevant, please leave a comment and a maintainer will re-triage it.
            It will be automatically closed in 14 days if there is no further activity.
          close-issue-message: |
            This issue has been closed due to inactivity.
            If the problem persists, please open a new issue with fresh information.
          close-issue-reason: not_planned

          days-before-pr-stale: 14
          days-before-pr-close: 7
          stale-pr-label: 'stale'
          stale-pr-message: |
            This pull request has been inactive for 14 days.
            Please rebase on the latest `main` and address any open review comments.
            It will be automatically closed in 7 days if there is no further activity.
          close-pr-message: |
            This PR has been closed due to inactivity. Feel free to re-open when ready to continue.

          exempt-issue-labels: 'pinned,security,good first issue,help wanted,blocked'
          exempt-pr-labels: 'pinned,security,work-in-progress'
          exempt-all-assignees: true
